# This file consists of expressions

# rvalues
"abc";
5; 
true;
false;
3.14;
nil;
new Person;
x;
x.y;
x.y.z;
f();
f(1);
f(1,2);
f("a",1,"b");
f(x);
f(x, y);
f(x, y, z);
f(x.y.z, 1);

# parenthesized rvalue expressions 
(5);
((5));
(((true)));

# mathrel
5 + 4;
"a" + "b" + true;
5 + 4 + 3 + "foo";
5 + 4 - 3 / 1 * 2 % 1;
2 + f(1) - f(1, 2);

# full parenthesized expressions
(5 + 3);
5 + (3 * 2);
(5 + 3) * 2;
((4 / (1 + 1)) + 1);
(4 / (1 + (1 + 1)));
f(((x + y) / 2) + f(1, 2));

